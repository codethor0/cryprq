FROM electronuserland/builder:wine-mono

WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm ci

# Copy the rest of the GUI code
COPY . .

# Default command builds unsigned Windows NSIS
CMD ["npm", "run", "build:win-unsigned"]

